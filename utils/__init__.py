"""Cross-platform utility helpers for the PAI assistant.

Provides platform detection and app resolution (``platform_utils``),
browser/CDP discovery (``browser_utils``), and screenshot capture
(``vision_utils``). These modules isolate OS-specific branching so the
rest of the codebase can stay platform-agnostic.
"""
